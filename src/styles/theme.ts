import { DefaultTheme } from 'styled-components';
import { orange } from './colors';

const myTheme: DefaultTheme = {
  borderRadius: '25px',

  colors: {
    main: orange,
    secondary: 'magenta',
  },
};

export { myTheme };